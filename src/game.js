class Game {
    
    constructor ({id, score, difficulty, player, created_at, player_id}) {
        this.id = id
        this.score = score
        this.difficulty = difficulty
        this.player_id = player_id
        this.player = player
        this.created_at = created_at
    }

}